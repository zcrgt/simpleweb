<h1>Employees</h1>

<img src="<%= @image_url %>" alt="">

<table>
  <thead>
    <th>Name</th>
    <th>Job Title</th>
  </thead>
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td>
          <%= user.name %>
        </td>
        <td>
          <%= user.job_title %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
